# README.md

Run an example using in the current directory
```
uv run main.py ./../../.build-repos/vision-transformers/vit-cifar10.ipynb .build/test/a.md
```

For use with 1bbb, try
```
uv run --script tools/convert-notebooks/main.py <input> <output>
```